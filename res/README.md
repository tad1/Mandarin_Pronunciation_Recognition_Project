# Results

all of generated/processed data should be saved here (plots, cached indexes, cleaned datasets)

it MUST be safe to remove all of the files in `/res` directory.
meaning, the code (`/src`) CAN use any of `/res` data, but it SHOULD'T rely on its existence
the purge of `/res` should be equalivent to a clean build/run
